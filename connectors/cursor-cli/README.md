# Cursor CLI Switch integration

Switch Console supplies the room workflow and MCP server to local Cursor ACP sessions. Uses the installed `agent` CLI and its existing login. No standalone connector install is required. Updates ship with Console.
