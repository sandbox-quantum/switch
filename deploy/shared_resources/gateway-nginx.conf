server {
    listen 3000;

    root /usr/share/nginx/html;
    index index.html;

    location = /gateway/provider-connections/github/callback {
        access_log off;
        proxy_pass http://switch:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /gateway {
        proxy_pass http://switch:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
